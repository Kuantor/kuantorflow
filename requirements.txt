Flask==3.0.3
mysql-connector-python==9.0.0
python-dotenv==1.0.1
beautifulsoup4==4.12.3
lxml==6.1.1
requests==2.32.3

# Optional "Sign in with Google" (OAuth 2.0 / OpenID Connect). If unset,
# the feature disables itself — see GOOGLE_CLIENT_ID in app.py.
Authlib==1.7.2

# Mykola AI chat — imported from the sibling ai_agent repo (see AI_AGENT_PATH).
# These are the agent's runtime deps; without them Mykola is simply disabled.
anthropic==0.116.0
scikit-learn==1.9.0
