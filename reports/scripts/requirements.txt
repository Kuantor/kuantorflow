# Dependencies for the build-time converters (reports/scripts/).
# Install into whatever environment you run the converters from:
#   pip install -r reports/scripts/requirements.txt
python-docx==1.2.0
# md_to_pdf.py reads the finished PDF back before reporting success (issue
# #211): headless Edge will happily print its own error page, and that is a
# structurally valid PDF, so nothing cheaper than reading the text catches it.
pypdf==6.14.2
# to_webp.py (#234). Already present in the app venv, but named here because
# this folder is run from whatever environment is to hand, not only that one.
Pillow==12.3.0
